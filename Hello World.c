#include<stdio.h>
int main()
{
    int a,T,n,n2,n3;
    scanf("%d",&T);
    for(a=0; a<4; a++)
    {
        if (T>=1 && T<=100)
        {
            scanf("%d",&n);
        }

    }
    printf("fail");

    for(a=0; a<4; a++)
    {
        if (T>=1 && T<=100)
        scanf("%d",&n2);
        if (n2<40)
        {
        }
    }for(a=0; a<4; a++)
    {
        if (T>=1 && T<=100)
        scanf("%d",&n3);
        if (n3<40)
        {
        }

    printf("Pass");

}return 0;
}
